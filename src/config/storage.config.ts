export const STORAGE_KEYS = Object.freeze({
  accessToken: "workey_access_token",
  refreshToken: "workey_refresh_token",
  user: "workey_user",
  theme: "workey_ui_theme",
  locale: "workey_locale",
} as const)
